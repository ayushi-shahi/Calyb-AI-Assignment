{
  "_note": "Definitions written in plain language. Read through once so you can explain each in an interview, and tweak any that don't sound like how you'd say it. Fill introduced_by during annotation with the real paperId from your landed corpus (don't guess).",

  "concepts": [
    {"id": "proximity-graph", "name": "Proximity Graph", "category": "index-structure",
     "definition": "Connect each vector to a handful of its closest neighbors, and searching just becomes walking across those links toward whatever you're looking for.", "introduced_by": null},
    {"id": "hierarchical-layering", "name": "Hierarchical Layering", "category": "index-structure",
     "definition": "Stack several graphs on top of each other — a sparse one on top for big jumps, the full one at the bottom — so you zoom in from rough to exact.", "introduced_by": null},
    {"id": "navigable-small-world", "name": "Navigable Small World", "category": "index-structure",
     "definition": "A graph that mixes mostly-local links with a few long shortcuts, so you can hop from anywhere to your target in very few steps (the 'six degrees of separation' idea).", "introduced_by": null},
    {"id": "kd-tree", "name": "KD-Tree", "category": "index-structure",
     "definition": "Split the space in half again and again, one axis at a time, into a tree you can search fast — great in 2D/3D, but falls apart once dimensions get high.", "introduced_by": null},
    {"id": "inverted-file-index", "name": "Inverted File Index (IVF)", "category": "index-structure",
     "definition": "Group all vectors into clusters up front, then at query time only look inside the few clusters nearest the query instead of scanning everything.", "introduced_by": null},
    {"id": "hash-bucket-index", "name": "Hash Bucket Index", "category": "index-structure",
     "definition": "Use a hash function built so similar vectors fall into the same bucket, then only compare against what's in the query's bucket.", "introduced_by": null},

    {"id": "product-quantization", "name": "Product Quantization", "category": "compression",
     "definition": "Chop each vector into pieces, swap each piece for the closest entry in a tiny codebook, and store just those short codes instead of the full vector — big memory savings.", "introduced_by": null},
    {"id": "optimized-product-quantization", "name": "Optimized Product Quantization", "category": "compression",
     "definition": "Product quantization, but rotate the data first so the pieces split more cleanly, which cuts the accuracy you lose to compression.", "introduced_by": null},
    {"id": "additive-quantization", "name": "Additive Quantization", "category": "compression",
     "definition": "Instead of chopping the vector, rebuild it as a sum of codewords from several codebooks — more accurate than PQ, but slower to encode.", "introduced_by": null},
    {"id": "scalar-quantization", "name": "Scalar Quantization", "category": "compression",
     "definition": "Just squash each number in the vector down to fewer bits (like 32-bit floats to 8-bit ints) to save space.", "introduced_by": null},
    {"id": "binary-codes", "name": "Binary Codes", "category": "compression",
     "definition": "Turn each vector into a short string of 0s and 1s so you can compare them with lightning-fast bit operations.", "introduced_by": null},

    {"id": "greedy-graph-traversal", "name": "Greedy Graph Traversal", "category": "search-strategy",
     "definition": "Start at some node and keep jumping to whichever neighbor is closest to the query, stopping when no neighbor is any closer.", "introduced_by": null},
    {"id": "beam-search", "name": "Beam Search", "category": "search-strategy",
     "definition": "Instead of following one path, keep a small set of the best candidates alive at once — more work, but better answers.", "introduced_by": null},
    {"id": "multi-probe", "name": "Multi-Probe", "category": "search-strategy",
     "definition": "When using hash buckets, also peek into a few neighboring buckets, since a real neighbor might have just barely landed in the wrong one.", "introduced_by": null},
    {"id": "coarse-to-fine-search", "name": "Coarse-to-Fine Search", "category": "search-strategy",
     "definition": "Do a cheap rough pass to shortlist a small area, then a careful pass only within that area.", "introduced_by": null},
    {"id": "re-ranking", "name": "Re-ranking", "category": "search-strategy",
     "definition": "After a fast approximate search hands you a shortlist, recompute exact distances on just those few to fix the ordering.", "introduced_by": null},
    {"id": "non-exhaustive-search", "name": "Non-Exhaustive Search", "category": "search-strategy",
     "definition": "Any method that deliberately looks at only a slice of the data per query instead of scanning all of it.", "introduced_by": null},

    {"id": "euclidean-distance", "name": "Euclidean Distance", "category": "distance-metric",
     "definition": "The plain straight-line distance between two points.", "introduced_by": null},
    {"id": "cosine-similarity", "name": "Cosine Similarity", "category": "distance-metric",
     "definition": "How similar the direction of two vectors is, ignoring their length — usually what you want for embeddings.", "introduced_by": null},
    {"id": "inner-product", "name": "Inner Product", "category": "distance-metric",
     "definition": "The dot product; used when a bigger score means more relevant, like in recommendations and maximum-inner-product search.", "introduced_by": null},
    {"id": "asymmetric-distance-computation", "name": "Asymmetric Distance Computation", "category": "distance-metric",
     "definition": "Keep the query as a full-precision vector while the database side stays compressed, so you lose less accuracy than compressing both.", "introduced_by": null},
    {"id": "hamming-distance", "name": "Hamming Distance", "category": "distance-metric",
     "definition": "Count how many bits differ between two binary codes — that count is the distance.", "introduced_by": null},

    {"id": "disk-resident-index", "name": "Disk-Resident Index", "category": "hardware-optimization",
     "definition": "Keep most of the index on an SSD and only a little in RAM, so you can handle far more vectors than would ever fit in memory.", "introduced_by": null},
    {"id": "gpu-acceleration", "name": "GPU Acceleration", "category": "hardware-optimization",
     "definition": "Push the heavy distance math onto a GPU to run thousands of comparisons in parallel.", "introduced_by": null},
    {"id": "memory-footprint-reduction", "name": "Memory Footprint Reduction", "category": "hardware-optimization",
     "definition": "A catch-all for tricks whose main goal is cramming more vectors into the same amount of RAM.", "introduced_by": null},
    {"id": "cache-locality", "name": "Cache Locality", "category": "hardware-optimization",
     "definition": "Arrange the data so the pieces you touch during a search sit close together and stay in the CPU's fast cache.", "introduced_by": null}
  ]
}
